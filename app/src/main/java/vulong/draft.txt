home -> sharedViewModel
task -> sharedViewModel

//click 1 task in home
home -taskId-> task:
    task -> getIdFromDb

//từ task scr về home
task -(delete):taskId->home   delete trực tiếp xong chạy lệnh repo,
task -(noAction)->home
task -(newTask)->home:getAll  taskTemp để add nhanh animation, xong chạy lệnh repo
task -(update)->home:getAll     update trực tiếp trong list, xong chạy lệnh repo

search scr

